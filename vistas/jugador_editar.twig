<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8">
		<title>Editar jugador</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
		<link rel="stylesheet" href="/vistas/styles/home.css?v=1">
	</head>

	<body>

		<div class="container py-4">
			<div class="d-flex justify-content-end gap-2 mb-4 top-buttons">
				<a href="/home" class="btn btn-outline-light btn-sm">Home</a>
				<a href="/logout" class="btn btn-danger btn-sm">Cerrar sesión</a>
			</div>

			<h1 class="text-center mb-4 title-glow">Editar Jugador</h1>

			<div class="inazuma-card p-4 rounded">

				<form action="/jugador/actualizar" method="POST">

					<input type="hidden" name="id" value="{{ jugador.codJug }}">

					<div class="mb-3">
						<label class="form-label">Nombre</label>
						<input type="text" name="nombre" class="form-control" value="{{ jugador.nombre }}" required>
					</div>

					<div class="mb-3">
						<label class="form-label">Apellido</label>
						<input type="text" name="apellido" class="form-control" value="{{ jugador.apellido }}" required>
					</div>

					<div class="mb-3">
						<label class="form-label">Dorsal</label>
						<input type="number" name="dorsal" class="form-control" min="1" max="99" value="{{ jugador.dorsal }}" required>
					</div>

					<div class="mb-3">
						<label class="form-label">Posición</label>
						<select name="posicion" class="form-select" required>
							<option value="DL" {{ jugador.posicion == "DL" ? "selected" }}>Delantero</option>
							<option value="MD" {{ jugador.posicion == "MD" ? "selected" }}>Centrocampista</option>
							<option value="DF" {{ jugador.posicion == "DF" ? "selected" }}>Defensa</option>
							<option value="POR" {{ jugador.posicion == "POR" ? "selected" }}>Portero</option>
						</select>
					</div>

					<div class="mb-3">
						<label class="form-label">Elemento</label>
						<select name="elemento" class="form-select" required>
							<option value="FUEGO" {{ jugador.elemento == "FUEGO" ? "selected" }}>🔥 Fuego</option>
							<option value="AIRE" {{ jugador.elemento == "AIRE" ? "selected" }}>🌪️ Aire</option>
							<option value="MONTANIA" {{ jugador.elemento == "MONTANIA" ? "selected" }}>🏔️ Montaña</option>
							<option value="BOSQUE" {{ jugador.elemento == "BOSQUE" ? "selected" }}>🌳 Bosque</option>
						</select>
					</div>

					<div class="mb-3">
						<label class="form-label">URL Imagen</label>
						<textarea name="imagen" class="form-control" rows="3">{{ jugador.imagen }}</textarea>
					</div>

					<button class="btn btn-warning w-100">Guardar cambios</button>

				</form>

			</div>

		</div>

	</body>
</html>
